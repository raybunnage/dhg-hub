[pytest]
markers =
    integration: marks tests as integration tests
    requires_database: marks tests that require a database connection
asyncio_mode = auto
python_files = test_*.py
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning 